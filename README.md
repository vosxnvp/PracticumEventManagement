# PracticumEventManagement

Учебный проект ASP.NET Core Web API для управления мероприятиями.

Проект выполнен в рамках первого спринта курса «Продвинутая разработка на C# и .NET».

## Технологии

- C#
- .NET 10
- ASP.NET Core Web API
- Swagger / OpenAPI
- In-memory хранилище данных

## Запуск проекта

Клонировать репозиторий:

```bash
git clone https://github.com/vosxnvp/PracticumEventManagement.git
```

Перейти в каталог проекта:

```bash
cd PracticumEventManagement/PracticumEventManagement
```

Собрать проект:

```bash
dotnet build
```

Запустить приложение:

```bash
dotnet run
```

После запуска адрес приложения будет указан в консоли.

Swagger UI доступен по адресу:

```text
http://localhost:5132/swagger
```

Порт может отличаться в зависимости от настроек запуска.

## API

### Получить все мероприятия

```http
GET /events
```

### Получить мероприятие по идентификатору

```http
GET /events/{id}
```

Если мероприятие не найдено, возвращается:

```text
404 Not Found
```

### Создать мероприятие

```http
POST /events
```

Пример запроса:

```json
{
  "title": "Встреча команды",
  "description": "Обсуждение проекта",
  "startAt": "2026-08-10T10:00:00",
  "endAt": "2026-08-10T11:00:00"
}
```

При успешном создании возвращается:

```text
201 Created
```

### Обновить мероприятие

```http
PUT /events/{id}
```

Пример запроса:

```json
{
  "title": "Обновлённая встреча",
  "description": "Обновлённое описание",
  "startAt": "2026-08-10T12:00:00",
  "endAt": "2026-08-10T13:00:00"
}
```

При успешном обновлении возвращается:

```text
204 No Content
```

Если мероприятие не найдено:

```text
404 Not Found
```

### Удалить мероприятие

```http
DELETE /events/{id}
```

При успешном удалении возвращается:

```text
204 No Content
```

Если мероприятие не найдено:

```text
404 Not Found
```

## Валидация

При создании и обновлении мероприятия проверяется:

- наличие `Title`;
- наличие `StartAt`;
- наличие `EndAt`;
- `EndAt` должен быть позже `StartAt`.

При ошибках валидации API возвращает:

```text
400 Bad Request
```

## Хранение данных

Данные хранятся в памяти приложения.

После остановки и повторного запуска приложения созданные мероприятия удаляются.
